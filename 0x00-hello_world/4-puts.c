#include <stdio.h>
/**
 * main - This program prints using the put function
 * put_prnt recieves the string to be printed
 *
 * Return: a non negative integer (0)
 */
int main(void)
{
	char put_prnt[] = "Programming is like building a multilingual puzzle";

	puts(put_prnt);
	return (0);
}
